# - Chứa đầy đủ tất cả các thư viện cần thiết cho dự án.
# - Có thể sử dụng để cài đặt môi trường hoàn chỉnh với lệnh: pip install -r requirements.txt

# --- Thư viện PyTorch (CPU/GPU) ---
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=0.15.0

# --- Thư viện giao diện ---
PySide6>=6.5.0

# --- Thư viện xử lý ảnh & AI ---
Pillow>=9.0.0
ImageHash>=4.3.0
open-clip-torch>=2.20.0
huggingface-hub>=0.16.0
opencv-contrib-python-headless>=4.8.0
faiss-cpu>=1.7.4 # For faster similarity search on CPU

# --- Thư viện tiện ích ---
tqdm>=4.65.0
send2trash>=1.8.2
python-dotenv>=1.0.0
requests>=2.31.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
